//Holds are your configs vars
var ip = "127.0.0.1";
var port = "80";

//Packages them up
var configs = {
	ipaddress : ip,
	port : port,
    api_key : "no key here"
}

//Exports them
module.exports = configs;
